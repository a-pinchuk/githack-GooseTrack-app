import { Modal } from 'components/Modal/Modal';

export const AddFeedbackModal = ({ handlerCloseModal }) => {
  return (
    <Modal handlerCloseModal={handlerCloseModal}>
      <p>AddFeedbackModal</p>
    </Modal>
  );
};
